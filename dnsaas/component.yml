name: dnsaas
file: dnsaas.py
description: DNS as a service

Services:
    - name: install
      description: Install designate and PowerDNS
      methods:
          - install
    - name: start
      description: Start PowerDNS and Designate services
      methods:
          - start
    - name: set_config_file
      description: Configure the PowerDNS and Designate services
      methods:
          - set_config_file

