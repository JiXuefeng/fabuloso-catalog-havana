name: dnsaas
file: dnsaas.py
description: DNS as a service

Services:
    - name: install
      description: Install and configure component file
      methods:
          - install
          - start
    - name: start
      description: Start PowerDNS and Designate service
      methods:
          - start